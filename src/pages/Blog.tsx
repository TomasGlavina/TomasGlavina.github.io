import SectionTitle from "@/components/Title";

export default function Blog() {
  return (
    <section id="blog" className="scroll-mt-24">
      <SectionTitle emoji="📝">Blog</SectionTitle>
    </section>
  );
}
